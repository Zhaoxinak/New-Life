# 逻辑修订纪要 · 2026-08-11

通读事件+对话后的结构性修订（非纯润色）。权威以对话档案 + effect 词汇表为准。

## 断线 / 假成功（已修）

1. **E010B** 成功现在也给 `clue_light_crate`（否则无法进 E013）
2. **E010C** 推迟 → 新增 **E010b**（Day13 重入）
3. **E014** 去掉提前点破「鸦片」；命名留给 E015
4. **Route C**：E017/R009 读 `route_foreign_closed`；E018C 硬门 `flag_ending_c_ready`
5. **终局**：F003/F005 → `end_run` + 失败旗；E018 失败分支拆开（开除/清除/反水/数值）
6. **F004** → `flag_liu_betrayed`，E018 认旗

## 契约（已修）

- 疑心 `0–3` int；禁 `high`/`extreme`
- `liu_source_risk` 升为 **meter**（不再对 flag 做 add）
- `end_run` / `loc` require 写入词汇表
- 三条路线字母与 E009 对齐：A 隐忍 / B 跳槽 / C 洋人

## 戏剧 / 因果（已修）

- E001 开场弱化洋药剧透
- E009 选项去系统腔
- E012 硬前置 E008B + score≥40；拒礼不再无故加商行嫌疑
- E004 / E017 outro / E018B / F004 / R003 加厚或改动机

## 金钱驱动力（已修）

- **规则层**：`主角属性` / `阈值与惩罚` / `设计原则` §6 定 stat_money 四用途与五档区间
- **人物层**：`_模板` §2 钱与生计；回填林/柳/钱子安/钱德茂/白/赵/王/周
- **事件层**：E002/E006/E008/E012/E014/E015/E018 + R001/R005/R006 + F002/F003
- **行动层**：ACT_01–12 均补「金钱戏」；`act_11` 展开 loan/remit/market/tier2 菜单；`06_行动档案/README` 索引表
- **口风池**：`DIALOG_ACT_POOL` — 12 行动 outro + 钱庄菜单 + 日末拮据提示
- **建筑层**：`LOC_03` / `LOC_04` 热区补金钱门槛与 loc_key 索引
- **契约层**：effect 词汇表 `menu` / `goto_dialog_by_condition`；旗标表补 `flag_money_*` / `flag_bank_loan_active`
- **地点层**：R001–R009 补 `loc` + `slot` require；LOC_01/02/06/05 热区补金钱戏

## 仍可后续做

- 机器可读 YAML 导出
