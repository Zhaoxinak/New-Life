# 存档结构（运行库契约）

> 存档 = 运行库快照。只存**实例状态**，不存定义正文、不存可派生量的权威副本。  
> 相关：[`数据库架构.md`](数据库架构.md) · [`effect词汇表.md`](effect词汇表.md)

---

## 1. 存什么 / 不存什么

| 存 | 不存 |
|---|---|
| 当前日、时段、槽位元数据 | 人物背景文案、性情长文 |
| `stat_*` 当前值 | 属性「含义说明」 |
| 每条边的 score / 修正 / 债 / 把柄 | 由 score 推导的 tier（可缓存） |
| meter、flag、已解锁线索与品质 | 事件完整对白 |
| 持有信物/把柄 | 建筑描述、热区说明 |
| 已读对话节点（可选） | Mod 定义原文 |
| 事件队列（未播完） | |

---

## 2. 槽位元数据 `run_meta`

| 字段 | 类型 | 说明 |
|---|---|---|
| `slot_id` | int | 0..N |
| `schema_version` | string | 如 `0.1`；读档校验 |
| `content_version` | string | 暗潮文档/数据包版本 |
| `day` | int | ≥1；**不锁 30**（三章开放日程） |
| `slot` | enum | morning/noon/afternoon/evening/late_night |
| `player_rank` | enum | `apprentice` \| `waichang` \| `paojie` \| `houtang`；初值 `apprentice` |
| `rng_seed` | int | 可复现随机 |
| `playtime_sec` | int | 可选 |
| `last_act_id` | act_id? | 可选，供 tick 判断 |

---

## 2b. 朝账 `run_meeting`

| 字段 | 类型 | 说明 |
|---|---|---|
| `cycle_index` | int | 已完成朝账次数 |
| `days_until_next` | int | 距下次朝账（朝账日结束置 7） |
| `report_score` | int | 本周汇报分 0–100 |
| `attendance_tier` | enum | `listen` \| `report` \| `decide` |
| `weekly_tasks` | array | `{ id, target, progress, weight }` |
| `last_summary_key` | string? | 上次朝账摘要 loc_key |
| `last_policy` | string? | 上次⑤定调口径（账簿回顾） |
| `council_queue` | array | ④ 段 `char_id` 发言顺序 |
| `council_index` | int | 当前轮到 queue 中第几位 |
| `council_log` | array | 本场发言/沉默记录 |
| `policy_draft` | object | 建言倾向累积分，供 ⑤ 定调 |

规则：[`../17_朝账系统/朝账系统规则.md`](../17_朝账系统/朝账系统规则.md) · 建言：[`../17_朝账系统/诸人建言.md`](../17_朝账系统/诸人建言.md)。

---

## 2c. 序位战 `run_ladder`

| 字段 | 类型 | 说明 |
|---|---|---|
| `pool_id` | enum | `pool_apprentice` \| `pool_waichang` \| `pool_paojie` |
| `cycle_week` | int | 本池第几周 |
| `promotion_slots` | int | 本朝账可升名额 |
| `entries` | array | `{ char_id, score, is_player, trend }` |
| `player_rank` | int | 玩家名次（缓存） |
| `player_total` | int | 池内人数 |
| `last_delta` | int | 较昨日名次变化（Hub ↑↓） |

规则：[`../13_职级档案/职级竞争与排名.md`](../13_职级档案/职级竞争与排名.md)。

---

## 3. 主角属性 `run_stat`

| key | 初值来源 | 备注 |
|---|---|---|
| `stat_money` | def | |
| `stat_intel` | def | |
| `stat_network` | def | |
| `stat_trust_firm` | def | 商行信任（职场，非交情边） |
| `stat_suspicion` | def | 主角嫌疑（全局） |
| `stat_support_mid` | def | |
| `stat_support_low` | def | |
| `stat_credit_bank` | def / 缺省 0 | 票号信用；决定借贷与高阶往来 |
| `stat_credit_market` | def / 缺省 0 | 街市信用；决定消息价与竞品试价 |
| `stat_credit_foreign` | def / 缺省 0 | 洋行信用；决定正式会谈与长期合作 |

缺 key 时按定义初值补齐（向前兼容）。

---

## 4. 关系边 `run_edge`

| 字段 | 说明 |
|---|---|
| `from` / `to` | `char_id` |
| `score` | −100..+100 |
| `trust` / `suspicion` / `fear` | 档位整数（见词汇表） |
| `debt` | 人情债/账外欠情；短字符串或短数组 |
| `leverage` | 把柄/担保/卡脖子；短字符串或短数组 |
| `bonds` | 开局可从定义拷贝；运行时很少改 |

未出现的边：读时视为默认（泛泛 / 0 / 低 / 低 / 无 / 无）。  
**不必存满笛卡尔积。**

---

## 5. Meter `run_meter`

| meter_id | 初值 | 说明 |
|---|---|---|
| `father_son` | 55 | 钱氏父子和睦 |
| `pursuit` | 0 | 柳↔子安追求 |
| `eval` | 30 | 白→钱业务评估 |
| `impression_bradley` | 0 | 白→林 |
| `impression_qing` | 0 | 庆→林 |
| `liu_source_risk` | 0 | 如烟渠道暴露风险 |

---

## 6. 旗标 `run_flag`

仅存 `true` 或非默认枚举。完整合法名见 [`旗标与命名.md`](旗标与命名.md)。

例：`route_endure` `flag_liu_spy` `flag_fired`…

---

## 6c. 履历 `run_history`

按时间追加的可读履历（账簿「往来」）。存实例摘要，不存对白全文。

```yaml
run_history:
  - day: 3
    slot: afternoon
    kind: event          # event | edge | rank | grudge | clue | debt | route
    ref: E004
    summary_key: event.E004.name   # 或 history.* / def loc_key
    payload: {}          # 可选短字段
```

| 规则 | 说明 |
|---|---|
| 写入点 | 事件结束、交情/线索/恩怨/债变更、升降职、路线旗标点亮 |
| 旧档 | 无 `history` 时读档为空；可用 `seen_event_*` 回填标题级条目 |
| 表现 | 账簿往来 Tab；禁止系统腔「+10」作摘要 |

---

## 6b. 恩怨 `run_grudge`

```yaml
run_grudge:
  - id: grudge_zian_slight
    status: open          # open | punished | forgiven | expired
    debtor: char_qian_zian
    buried_by: E004
```

定义与兑现窗见 [`../05_关系档案/恩怨账与清算.md`](../05_关系档案/恩怨账与清算.md)。

---

## 7. 债务实例 `run_debt`

```yaml
run_debt:
  debt_bank_short_001:
    service_id: svc_bank_loan_short
    creditor: org_bank
    principal: 15
    remaining: 12
    interest_per_day: 1
    due_day: 18
    collateral: none
    status: active
```

### 建议字段

| 字段 | 说明 |
|---|---|
| `service_id` | 对应定义服务 |
| `creditor` | `org_*` / `char_*` |
| `principal` | 初始借额 |
| `remaining` | 剩余应还 |
| `interest_per_day` | 日息 |
| `due_day` | 到期日 |
| `collateral` | 担保物/担保人 |
| `status` | `active` / `overdue` / `cleared` |

债务层用于正规借贷与明确借账；**人情债仍优先写在 `run_edge.debt`**。

---

## 8. 线索 / 信物

```yaml
run_clue:
  clue_suspicious_manifest: { owned: true, quality: partial }
run_item:
  item_qing_letter: { owned: true, bound_to: char_lin_ruisheng }
```

---

## 9. 组织实例（可选 · Demo 可砍）

```yaml
run_org:
  org_qianji:
    firm_bright: 55
    firm_dark: 40
    firm_heat: 25
    cash_bright: 60
    cash_dark: 45
    liquidity: 50
    payroll_pressure: 20
    tribute_pressure: 35
```

### 组织账务建议

| 字段 | 说明 |
|---|---|
| `cash_bright` | 明面现金流 |
| `cash_dark` | 黑账现金流 |
| `liquidity` | 周转宽紧 |
| `payroll_pressure` | 工资/伙计压力 |
| `tribute_pressure` | 上供/月奉压力 |

---

## 10. 读档规则

1. `schema_version` 不识别 → 拒绝或走迁移表  
2. 缺字段 → 用定义初值填充  
3. 未知 flag/meter → 保留但警告（Mod 向前） / Demo 可丢弃  
4. tier 一律由 score 重算后再进 UI  
5. 未知 `run_debt.service_id` → 保留原值并警告，避免吞债  

---

## 11. 版本

| 版本 | 内容 |
|---|---|
| **v0.1** | meta / stat / edge / meter / flag / clue / item |
| **v0.2** | 增加 `credit_*`、`run_debt`、组织账务示例 |
| **v1.0** | `day` 不锁 30；`player_rank`；`run_grudge` |
