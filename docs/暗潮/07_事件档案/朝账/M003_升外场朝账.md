# 升外场朝账 · `M003`

| 项 | Day 22 · morning · `loc_01` · 条件 |
| 对话入口 | [`dialog_m003_start`](../../11_对话档案/DIALOG_M003.md) |
| 槽位 | `chapter2.rankup`（与 E020* 同槽） |
| 内嵌 | **E020** / **E020B** / **E020C** 三选一 |

## 触发

与 [`E020_升任外场.md`](../主线/E020_升任外场.md) 相同门槛；`mutex_group: chapter2.rankup` 决定播哪条内嵌线。

| 候选 | 条件摘要 |
|---|---|
| E020 | A 线；信任≥45；情报≥20；非降职/开除 |
| E020B | B 线主爽；聚丰报价条件 |
| E020C | C 线主爽；洋行门路条件 |

## 摘要

章二主爽落在朝账 ③ 段仪典。主角首次**堂内汇报**（`attendance_tier: report`），升外场后进入 ④ **建言**（首次可选发言/不言），再 ⑤ 定调、⑥ 摊派。

## 六段式落点

| 段 | 内容 |
|---|---|
| ① | 点名；主角首次进堂 |
| ② | 主角汇报（可选口径：老实 / 报喜 / 藏一手） |
| ③ | → `dialog_e020_start` / `e020b` / `e020c` |
| ④ | **诸人建言**：周管事、王胖子发言；**玩家可选建言/不言**（教程二） |
| ⑤ | 路线定调（吸收 `policy_draft`） |
| ⑥ | 外场差事摊派 |

## Effects

以所选 E020* 文档为准；M003 壳额外：

```yaml
effects:
  - { op: set_flag, key: flag_meeting_report_eligible, value: true }
  - { op: set_meeting_tier, value: report }
  - { op: assign_weekly_tasks, ids: [task_street_watch, task_delivery] }
```

## 备注

- B/C 若主爽在街市/洋行，③ 段后可 `goto_dialog` 离堂短场，再回朝账 ④–⑥。  
- E021* 仍在之后独立槽位，不在 M003 内嵌。
