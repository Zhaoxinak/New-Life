# 聚丰报价 · `E020B`

| 项 | 内容 |
|---|---|
| 建议日程 | 章二中后段 · afternoon · `loc_03`（茶楼侧座 / 聚丰侧门叙事） |
| 与 E020 互斥 | 本章二升阶仪式 **三选一**：E020 / E020B / E020C |
| 触发 | `route_defect`；`flag_zhao_contacted`；赵→林 `score>=35`；`stat_credit_market>=10` 或 `stat_network>=35`；`stat_intel>=20`；`rank=apprentice`；非 `flag_e020b_done` / `flag_e020c_done` / `flag_rank_waichang_ceremony`（若已走 A 仪式则本事件不抢戏，可降为数值-only 短节点——Demo 直接互斥） |
| 对话入口 | [`dialog_e020b_start`](../../11_对话档案/DIALOG_E020B.md) |

> **B 线章二主爽**：市场估价上涨。「可投位子」当面报价——不是钱记职称，是竞品把你标成资产。  
> 升阶四件套仍齐：仪式（赵当面开价）· 称呼（「可投的人」）· 银钱（定金/甜头）· 恩怨窗（E021B）。

## 金融 / 权限口径

| 层 | 变化 |
|---|---|
| 仪式 | 赵鸿运当面报价：位子 + 启动甜头口径 |
| 称呼 | 街市嘴碎改口「聚丰在谈他」 |
| 银钱 | 第一笔可见甜头（定金性质） |
| 权限 | 跳槽谈判解锁加深；客户名单开始值钱 |
| 职级 | 仍在钱记时可软升 `waichang`（差事更自由，方便带货情）；主爽在估值 |
| 恩怨窗 | `flag_grudge_window_light` → 走 E021B 皮 |

```yaml
effects:
  - { op: set_rank, value: waichang }
  - { op: add, key: stat_money, value: 6 }
  - { op: add, key: stat_credit_market, value: 10 }
  - { op: add, key: stat_network, value: 5 }
  - { op: add, edge: {from: char_zhao_hongyun, to: char_lin_ruisheng}, key: score, value: 10 }
  - { op: set_flag, key: flag_e020b_done, value: true }
  - { op: set_flag, key: flag_jufeng_offer_open, value: true }
  - { op: set_flag, key: flag_grudge_window_light, value: true }
```

说明：`stat_money +6` 是「报价附带的定金甜头」，不是月薪五两（那是 E018B）。
