# 升任外场 · `E020`

| 项 | 内容 |
|---|---|
| 建议日程 | Day 22 · morning · `loc_01` · 嵌入 **M003** 朝账 ③ 段 |
| 朝账壳 | [`M003_升外场朝账.md`](../朝账/M003_升外场朝账.md) |
| 与 E020B / E020C | **互斥**：章二升阶仪式三选一；若 B/C 主爽条件已满足，A 线本事件应回落为非主爽占位或不播 |
| 触发 | `rank=apprentice`；`stat_trust_firm >= 45`；`stat_intel >= 20`；非 `flag_demoted` / `flag_fired`；非 `flag_e020b_done` / `flag_e020c_done`；建议已 `flag_e019_done` 或情报≥25 |
| 对话入口 | [`dialog_e020_start`](../../11_对话档案/DIALOG_E020.md) |

> **职级爽（中段）**：升阶四件套必须齐。A 线示范；B/C 见 [`BC路线爽点映射.md`](../../10_路线与结局/BC路线爽点映射.md)。

## 金融 / 权限口径

| 层 | 变化 |
|---|---|
| 仪式 | 前堂宣布：林瑞生暂管外场跑差 |
| 称呼 | 可出现「林外场」 |
| 月例 | 碎银 → 外场月例微涨（叙事 + `stat_money` 首笔） |
| 权限 | 独立街市差事更稳；部分货单经手 |
| 恩怨窗 | 提示可进 E021 |

```yaml
effects:
  - { op: set_rank, value: waichang }
  - { op: add, key: stat_money, value: 4 }
  - { op: add, key: stat_trust_firm, value: 5 }
  - { op: add, key: stat_network, value: 5 }
  - { op: set_flag, key: flag_rank_waichang_ceremony, value: true }
  - { op: set_flag, key: flag_grudge_window_light, value: true }
```

无分支（仪式戏）；看客反应写入对白。若 `route_defect` 主导且赵报价更高，或 `route_foreign` 主导且洋行门路先成，则本事件让位给 E020B / E020C。
