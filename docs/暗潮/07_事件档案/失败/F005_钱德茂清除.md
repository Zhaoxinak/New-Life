# 钱德茂清除 · `F005`

| 项 | 内容 |
|---|---|
| 触发 | 钱→林 `suspicion` ≥ 4（杀意） |
| 对话入口 | [`dialog_f005_start`](../../11_对话档案/DIALOG_F005.md) |
| 终局 | 写 `flag_purged` + `flag_ending_fail`；**立即结束本局** |
| 缓冲 | 档位 3=极高时 tip 预警；日末若仍 ≥3 则 −1 衰减，避免压缩日程三连踩即死 |

```yaml
require:
  - { edge: {from: char_qian_demao, to: char_lin_ruisheng}, key: suspicion, op: ">=", value: 4 }
effects:
  - { op: set_flag, key: flag_purged, value: true }
  - { op: set_flag, key: flag_ending_fail, value: true }
  - { op: add, edge: {from: char_qian_demao, to: char_lin_ruisheng}, key: score, value: -50 }
  - { op: end_run, reason: purged }
```
