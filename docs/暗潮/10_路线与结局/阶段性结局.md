# 阶段性结局（章终）

> 交情读 **有向边 score/档**；父子和睦等读 **meter**。  
> 日程：三章开放，不锁具体日期；旧称 Day30 的口径一律以**章终**理解。权威节奏见 [`阶段爽点与职级阶梯.md`](阶段爽点与职级阶梯.md)。  
> 权威演出：[`../11_对话档案/DIALOG_E018.md`](../11_对话档案/DIALOG_E018.md)。

## 失败优先（任一即失败尾声）

| 条件 | 文案分支 |
|---|---|
| `flag_purged` | 清除离津（F005 已可提前 `end_run`） |
| `flag_fired` | 开除（F003 已可提前 `end_run`） |
| `flag_liu_betrayed` 或（柳 score≤−20 且 pursuit≥60） | 反水 |
| 嫌疑≥100 或 商行信任≤0 | 数值崩 |

## 成功结局

| 结局 | 条件 | 收益口径 |
|---|---|---|
| A 隐忍 | `father_son≤30` **且** 商行信任≥50 **且** 情报≥40 **且** 非 `route_endure_failed`；建议已 E022 或 `flag_marriage_agency_reclaimed` | `set_rank: paojie`；跑街月例、应酬账、钱记内部账权；`grudge_demao_defer` 任命戏兑现 |
| B 跳槽 | 人脉≥40 **且** 情报≥30 **且** 赵→林 `score≥40` | `flag_rank_jufeng_paojie`；新职位、启动资金、订单截胡空间 |
| C 洋人 | **`flag_ending_c_ready`** **且** 非 `route_foreign_closed` **且** qing/bradley≥30 **且** 情报≥35 **且** 中层支持≥40 | `flag_rank_foreign_agent`；结算场景 **`loc_05`**；分成/抽头/佣金见 [`LOC_05`](../02_建筑档案/LOC_05_宝顺洋行.md) |

多成功时可定「主导结局」或混搭尾声。未匹配成功且非失败旗 → 走数值失败分支。

章终前扫尾：仍 `open` 的 `grudge_*` 须兑现或 `expire_grudge`（见恩怨账文档）。
